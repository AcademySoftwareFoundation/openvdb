vdb_tool:

  New Features:
  - Added new action "-soup2ls/soup2sdf/shrinkwrap", which generates a level set from a soup of polygons.
  - Added new action "-files", which (recursively) visits files in a directory structure that matches extensions or patterns
  - Added new action "-mesh2udf", which generates a symmetric narrow band of unsigned distances to a mesh.
  - Added new action "-quat2tri/q2t", which triangulates all quads in a mesh.
  - Added new action "-vol2mesh", which converts a scalar volume into an adaptive polygon mesh.
  - Added new action "-fog2vol", which converts a fog volume into an adaptive polygon mesh.
  - Added new action "-slice", which generates one or more images of axis-aligned slices through a scalar grid.
  - Added new action "-img2mpeg", which converts multiple images to an mpeg movie file.
  - Added new action "-log", which logs warnings and (non-fatal) errors to a file.
  - Added new action "-sdf2udf", which converts a signed distance field into an unsigned distance field.
  - Added new action "-forAllValues/forOnValues,forOffValues", which applies simple compute kernels to all, active or in-active values in a grid.
  - Added triangulation of N-gons to Geometry::readOBJ and Geometry::readSTL, assuming they are convex.
  - Added read support for xyz files.

  Improvements:
  - Action -"read" now has optional support for reading OpenUSD files usd, usda, usdc, and usdz.
  - Action "-mesh2ls/mesh2sdf" now supports generation of asymmetric narrow bands.
  - Improved error reporting when using unsupported actions and options.
  - Actions can now have multiple names, e.g. "-read", "-import, "-load", "-i".
  - Added cutoff option to "-ls2fog" action.
  - Improved error handling in vdb_tool (will recover from many non-fatal errors)
  - Improved parsing of configuration files (tabs and tailing comments are now ignored)
  - Improved unit-tests and Doxygen documentation for all files related to the vdb_tool.

  Fixes:
  - Fixed issues in Geometry::readSTL so it works on all files in Thingi10K.
  - Fixed bug when reading ascii ply files and off files with normals.
  - Fixed several typos in the documentation of vdb_tool.